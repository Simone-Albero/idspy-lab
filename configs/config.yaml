defaults:
  - model: tabular_classifier
  - data: cic_2018_v2
  - loops: default
  - optimizer: adamw
  - scheduler: one_cycle
  - loss: classification
  - path: default
  - _self_

# Global settings
experiment_name: supervised_tab_classifier
seed: 42
device: auto  # auto, cpu, cuda, mps
prova: yess

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: False
