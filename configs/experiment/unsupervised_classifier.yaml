# @package _global_
defaults:
  - override /model: tabular_autoencoder
  - override /loss: reconstruction
  - override /loops: default
  - override /optimizer: adamw
  - override /scheduler: one_cycle

type: unsupervised_classifier

experiment:
  name: base
  exclude_background: false
  benign_tag: 'DDOS attack-HOIC'
  malicious_tag: null
  # malicious_tag: 'DoS attacks-Hulk'
  n_clusters: 7
