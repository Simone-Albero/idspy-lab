# @package _global_
pretraining: {}

defaults:
  - /model@pretraining.model: tabular_autoencoder
  - /loss@pretraining.loss: reconstruction
  - /model@fine_tuning.model: tabular_classifier
  - /loss@fine_tuning.loss: classification
  - override /loops: default
  - override /optimizer: adamw
  - override /scheduler: one_cycle
  - _self_

type: semi_supervised_classifier

experiment:
  # exclude_background: true
  exclude_background: false
  labeled_fraction: 0.005
  benign_tag: 'DDOS attack-HOIC'
  malicious_tag: null

fine_tuning:
  model:
    args:
      # num_classes: 10
      num_classes: 11
      hidden_dims: [128, 64, 32]
  frozen_depth: 2
